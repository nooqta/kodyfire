import ability from './ability'

export const canNavigate = to => to.matched.some(route => {
    const { meta } = route
    return ability.can(route.meta.action || 'read', route.meta.resource)
    //|| ability.can('manage', 'all')
})
// export const canNavigate = to => true