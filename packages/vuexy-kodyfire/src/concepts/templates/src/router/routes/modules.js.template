export default [
    <%=#each routes %>
    // *===============================================---*
    // *--------- <%= name %> -------------------------------*
    // *===============================================---*
    <%=#each children%>
    {
      path: '<%= path %>',
      name: '<%= pluralize ../name %>-<%= routeName %>',
      component: () => import('@/views/modules/<%= ../name %>/<%= ../name %>-<%= routeName %>/<%=  classify ../name %>{<%= classify routeName %>}.vue'),
      meta: {
        pageTitle: '<%= pluralize ../name %>',
         <%=#each meta %>
        <%= key %>: '<%= value %>',
        <%=/each %>
        breadcrumb: [
          {
            text: '<%= pluralize ../name %>',
            active: true,
          },
        ],
      },
    },
    <%=/each %>
      <%=/each %>
]